export declare const resolveAppFunction: (appFnId: string, opts?: ResolveOptions | undefined) => Promise<any>;
export declare type Resolver = (appFnId: string, opts: {
    basedir: string;
}) => string;
export interface ResolveOptions {
    basedir?: string;
    resolver?: Resolver;
}
