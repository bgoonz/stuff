import { OAuthToken } from "../entities/token.entity";
import { OAuthResponse, Options } from "./response";
export declare class BearerTokenResponse extends OAuthResponse {
    readonly accessToken: OAuthToken;
    readonly status: number;
    constructor(accessToken: OAuthToken, options?: Options);
}
