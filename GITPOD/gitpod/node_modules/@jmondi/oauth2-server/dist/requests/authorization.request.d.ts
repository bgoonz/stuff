import { OAuthClient } from "../entities/client.entity";
import { OAuthScope } from "../entities/scope.entity";
import { OAuthUser } from "../entities/user.entity";
import { GrantIdentifier } from "../grants/abstract/grant.interface";
export declare class AuthorizationRequest {
    readonly grantTypeId: GrantIdentifier;
    readonly client: OAuthClient;
    user?: OAuthUser | undefined;
    scopes: OAuthScope[];
    isAuthorizationApproved: boolean;
    redirectUri: string | undefined;
    state?: string;
    codeChallenge?: string;
    codeChallengeMethod?: string;
    constructor(grantTypeId: GrantIdentifier, client: OAuthClient, redirectUri?: string, user?: OAuthUser | undefined);
}
