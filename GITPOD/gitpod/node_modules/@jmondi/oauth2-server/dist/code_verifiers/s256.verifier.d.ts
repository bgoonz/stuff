import { ICodeChallenge } from "./verifier";
export declare class S256Verifier implements ICodeChallenge {
    readonly method = "S256";
    verifyCodeChallenge(codeVerifier: string, codeChallenge: string): boolean;
}
